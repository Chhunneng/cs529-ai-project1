"""LLM / OpenAI API integration (Agents SDK, templates, extraction).

Named ``llm`` so imports stay distinct from the third-party ``openai`` PyPI package
(``from openai import AsyncOpenAI`` vs ``from app.llm...``).
"""

__all__: list[str] = []
