:80 {
  encode gzip

  # Do NOT strip /api — backend expects /api/v1/...
  handle /api/* {
    reverse_proxy backend:8000
  }

  # Do NOT strip /healthz
  handle /healthz {
    reverse_proxy backend:8000
  }

  handle {
    reverse_proxy frontend:3000
  }
}